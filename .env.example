SUPABASE_URL=
SUPABASE_SERVICE_ROLE=
SUPABASE_ANON_KEY=
SUPABASE_STORAGE_BUCKET=product-images

# Optional admin emails (comma separated) that unlock admin UI when logged in via Supabase
ADMIN_EMAILS=

# Legacy admin password (not recommended, use Supabase Auth). If set, /api/admin/login works.
ADMIN_PASSWORD=

# Razorpay (optional)
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# Server
PORT=4000
